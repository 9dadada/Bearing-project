"""시간-주파수 변환 (경로 B/C 입력 생성).

- STFT 로 시작, 임펄스가 약하면 CWT 검토 (config 로 선택).
- n_fft / hop_length: 시간↔주파수 해상도 트레이드오프, 실험으로 결정.
- 진폭은 로그(dB) 스케일 (작은 변화도 살리기).
- 정규화는 정상 train 통계로만 (누수 차단).
- 멀티 샘플링 rate(12k·48k) 입력은 공통 입력 크기로 맞춤(리샘플링 또는 고정 시간길이).

TODO: to_spectrogram((N, L)) -> (N, 1, H, W).
"""
